{
  "name": "copilot-pipeline-sdk",
  "version": "1.0.0",
  "description": "Parallel pipeline orchestrator using the GitHub Copilot SDK",
  "main": "dist/pipeline.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/pipeline.js",
    "dev": "tsx src/pipeline.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@github/copilot-sdk": "0.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
